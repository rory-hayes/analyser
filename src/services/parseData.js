export function parseDataToGraph(data) {
    // Your existing parseDataToGraph implementation
    const nodes = [];
    const links = [];

    // Parse the data and create nodes and links
    
    return { nodes, links };
} 